#include <bits/stdc++.h>
using namespace std;

int main () {

    string str = "Local Hack Day";
 
    // Reverse str[begin..end]
    reverse(str.begin(), str.end());
 
    cout << str << endl;

    return 0;
}